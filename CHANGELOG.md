# v0.2.6
- Treat every nested `message/*` MIME part as an attachment boundary even when filename and Content-Disposition metadata are absent. Unmarked forwarded-email bodies no longer enter parent evidence or manufacture a mixed RFI role; they receive a stable fallback name and remain available for explicit selected import. Reuse Python's standard email MIME tree, advance parser version to `multisource-39`, and add no dependency, recursion, mailbox access or model call.
- Preserve every distinct exact RFI or Submittal reference when multiple workflow markers occur on one source line. Continue the existing bounded pattern scan after each marker start, keep canonical deduplication and digit-bearing validation unchanged, advance parser version to `multisource-38`, and add no dependency, fuzzy relationship inference or model call.
- Accept email-thread identifiers only as bounded RFC-style angle-bracketed `<local@domain>` tokens. Invalid `Message-ID`, `In-Reply-To` or `References` words now remain standalone instead of producing false hash links; valid multi-value and conflict behavior is unchanged. Reuse Python's standard email parser and the existing hash graph, advance parser version to `multisource-37`, and add no dependency, mailbox access, semantic inference or model call.
- Require an explicit Email Subject to contain an identifier accepted by the existing digit-bearing RFI/Submittal normalizer before it can outrank an exact body heading. Workflow-like Subjects without an accepted ID remain visible header evidence but cannot suppress a valid body primary. Reuse the shared normalizer, advance parser version to `multisource-36`, and add no dependency, fuzzy matching, mailbox access or model call.
- Select exactly one non-empty body inside each `multipart/alternative` group, preferring plain text then safe HTML. Conflicting same-type candidates now keep the first, mark the Email partial and never manufacture a mixed RFI/Submittal role; attachment inventory still stops at attachment boundaries. Reuse Python's standard email tree, advance parser version to `multisource-35`, and add no dependency, mailbox access or model call.
- Preserve documented common RFI values under exact `Status` or `RFI Status` labels across TXT, PDF and Email evidence plus the reviewer index. Status never manufactures Question/Response or authority; status conflicts remain ambiguous without precedence, metadata/prose stays neutral, parser version advances to `multisource-34`, and no dependency or model call is added.
- Recognize known Submittal dispositions under exact `Review Response`, `Final Response` and `Submittal Response` labels through the existing status map. Similar metadata stays neutral, conflicting responses stay ambiguous, parser version advances to `multisource-33`, and no custom-response engine, spreadsheet ingestion, dependency or model call is added.
- Treat an exact `Official Response` heading as an RFI response across text, email, PDF-page and reviewer-workflow paths, while `Official Response Date`, `Suggested Answer` and prose remain neutral. Reuse one shared role normalizer, advance parser version to `multisource-32`, and add no vendor status engine, dependency, model call or inferred response.
- Recognize exact `Furnish as Submitted`, `Furnish as Corrected` and `Amend and Resubmit` Submittal return stamps through the existing `APPROVED`, `APPROVED AS NOTED` and `REVISE AND RESUBMIT` canonical groups. Reuse the shared regular expression, status map and rejected-source guard, advance parser version to `multisource-31`, and add no dependency, model call or inferred approval from ordinary prose.
- Preserve exact compact RFI and Submittal identifiers with alphabetic project or discipline prefixes, including `ARC-0042`, `MEP-023` and `SUB-001`, across body text, Subject, filename fallback and workflow indexing. Distinct prefixes remain distinct; identifiers still require a digit and reject malformed separators, prose and email suffixes. Reuse the shared normalizer, advance parser version to `multisource-30`, and add no dependency, fuzzy matching or model call.
- Separate an exact plain-text `-- ` delimiter and Gmail `gmail_signature` wrapper into reviewable `EMAIL > SIGNATURE` evidence. Signature text no longer routes RFI/Submittal relationships or enters model extraction, while text after a closed HTML signature resumes as current. Reuse the standard-library parser and existing local skip path, advance parser version to `multisource-29`, and add no dependency, mailbox access or model call.
- Recognize exact Gmail `gmail_quote`/`gmail_quote_container` classes and Outlook `divRplyFwdMsg` HTML reply wrappers as quoted history. Matching nested wrappers retain reviewable evidence, current text resumes after the close, prior RFI/Submittal state cannot alter the current message, and wrapped history closes locally before extraction. Reuse Python's standard-library HTML parser, advance the parser version to `multisource-28`, and add no dependency, mailbox access or model call.
- Retain email headers and quoted history as reviewable evidence while completing both locally as `NO_REQUIREMENTS` before extraction batching. A synthetic RFI reply drops from three extraction batches to one without changing its published material candidate; Coverage exposes the skipped-fragment count. No dependency, model call, customer file or database migration is added.
- Add bounded local Outlook MSG parsing and selected-attachment import by reusing the MIT-licensed `python-oxmsg` package and CIRP's existing EML evidence, workflow, hash-only thread, capacity, SHA-256 and deduplication paths. Public upstream fixtures validated a message with an attachment, one without attachments and an invalid container; no mailbox, model, credential, customer file or paid API was used. The parser version advances to `multisource-27`.
- Reject email-address suffixes at the shared identifier normalizer and remove complete addresses before exact reference extraction only when bounded source text contains `@`. Compact local parts such as `rfi43@example.test` or `submittal25@example.test` cannot create false groups, while a later real identifier on the same line remains visible. The parser version advances to `multisource-26`; no dependency, mailbox access, model call or semantic inference is added.
- Exclude routing-neutral From, To, Cc and Date headers from exact workflow-reference extraction, preventing addresses such as `rfi42@example.test` from creating false reviewer groups. Subject, current body and explicit quoted-history references remain supported, header evidence remains visible, the parser version advances to `multisource-25`, and no dependency, mailbox access, model call or semantic inference is added.
- Preserve every distinct canonical Submittal status from one TXT or current Email source and expand the bounded combined value into the existing single-source conflict calculation. Conflicting statuses now reach the reviewer as `AMBIGUOUS` instead of silently keeping the first status. Reuse the existing normalizer and workflow state logic, advance the parser version to `multisource-24`, and add no dependency, model call or semantic inference.
- Accept `Request for Information`/`RFI` and `Submission`/`Submittal` consistently in bounded filename fallback for scanned or generic-content files. Keep Subject and body precedence unchanged, reuse the existing identifier normalizer, advance the parser version to `multisource-23`, and add no dependency, model call or semantic inference.
- Require RFI role words to be standalone headings or use an explicit colon, hash or dash separator. `Request for Information`, `Response time` and `Questionnaire` therefore remain neutral instead of manufacturing QUESTION, RESPONSE or MIXED roles. Reuse the shared deterministic pattern, advance the parser version to `multisource-22`, and add no dependency, model call or semantic inference.
- Preserve an exact Submittal relationship when a supported document uses `Submission No. 23-01` instead of `Submittal 23-01`. Reuse the existing reference extractor and identifier normalizer, advance the parser version to `multisource-21`, and add no dependency, model call or semantic inference.
- Preserve an exact RFI relationship when a supported document uses `Request for Information No. 0042` instead of the `RFI 0042` abbreviation. Reuse the existing reference extractor and identifier normalizer, advance the parser version to `multisource-20`, and add no dependency, model call or semantic inference.
- Route a generic-subject email or text document by its earliest explicit RFI/Submittal body heading instead of always preferring a later RFI. Explicit workflow Subject precedence and exact reference handling remain unchanged. Reuse existing regular-expression match positions, advance the parser version to `multisource-19`, and add no dependency, model call or semantic inference.
- Keep an RFI role or Submittal status inside its exact identifier section so a weaker body reference cannot relabel a stronger Subject or earlier heading. Each explicit body section now carries its own identifier and role/status evidence locator, while repeated occurrences of the primary identifier retain established behavior. Reuse deterministic parsing and advance the parser version to `multisource-18`; no dependency, model call or semantic inference is added.
- Mark an email with multiple distinct `Message-ID` values as `AMBIGUOUS` instead of silently treating it as an ordinary standalone message. Persist only a boolean conflict marker, retain the existing hash-only privacy boundary, leave missing-ID email valid, and advance the parser version to `multisource-17`; no dependency, mailbox access, model call or semantic inference is added.
- Show the true workflow-group total in the browser and let reviewers append bounded 500-group pages without duplicates. Reuse the existing pagination endpoint, page state and Load more interaction; do not auto-fetch the entire project, call a model, add cost or introduce a dependency. Also restore native `hidden` behavior where the existing button display rule previously overrode it.
- Recognize explicit RFI and Submittal Subjects behind up to six interleaved `Re`/`Fw`/`Fwd` or bracketed enterprise security labels such as `[EXTERNAL]` and `[External Email]`. Each bracketed label remains capped at 40 characters, and explicit workflow words plus digit-bearing identifiers are still required. Reuse Python regular expressions, advance the parser version to `multisource-16`, and add no dependency or semantic inference.
- Route primary workflow by explicit email Subject before explicit body heading before filename. A weaker source cannot relabel the document, while different exact identifiers remain references; this prevents a Submittal email from becoming RFI because of its body or filename. Reuse the existing deterministic patterns, advance the parser version to `multisource-15`, and add no dependency, model call or semantic inference.
- Stop parent-email MIME traversal at every attachment boundary. An attached `.eml` body and its nested files no longer leak into parent evidence, attachment inventory or RFI role before explicit import. Reuse Python's standard-library MIME parts and the existing attachment-boundary rule, advance the parser version to `multisource-14`, and add no dependency, mailbox access or model call.
- Treat an explicit Outlook-style `From:` block as quoted history when a `Sent:`, `Date:`, `To:` or `Subject:` label is on the same visible HTML line as well as on a following line. This prevents an older quoted RFI question from changing the current reply role to `MIXED`. Reuse the existing standard-library HTML and email parser, advance the parser version to `multisource-13`, and add no dependency, model call or mailbox access.
- Treat whitespace-only `text/plain` MIME alternatives as absent so a non-empty safe HTML alternative supplies Email, RFI or Submittal body evidence instead of being silently discarded. Any non-empty plain text remains preferred and alternatives are not merged. Advance the parser version to `multisource-12`; no HTML execution, remote fetch, dependency or model call is introduced.
- Order email-thread members from exact local hashed Message-ID parent/reference edges so ancestors appear before replies even when filenames sort in the opposite direction. Deterministic filename/ID order remains the tie-breaker for siblings, and cyclic header relationships are marked `AMBIGUOUS` without dropping any message. Reuse Python's standard-library heap and existing hash graph; no body inference, date guess, dependency or model call is added.
- Bound workflow-review reads to nine relationship fields using Python's bundled SQLite JSON functions instead of transferring and decoding complete parser summaries with page, vision, CAD and geometry arrays. A reproducible 2,000-page synthetic fixture reduced the returned JSON from 1,112,749 to 254 bytes (99.98%) and the best of three 300-iteration query/decode runs from 599.8 ms to 255.1 ms (2.35x). Legacy top-level workflow fields and endpoint output remain covered; no dependency, model call or customer file is used.
- Harden deterministic workflow relationships: a Submittal primary plus an exact reference now displays as `LINKED` instead of an unexplained `OPEN`; an email that references its own exact hashed Message-ID is `AMBIGUOUS`; and selected-attachment provenance is ignored unless the parent document parsed as email. Existing state, hash and provenance structures are reused with no dependency or model call.
- Keep page-level `UNKNOWN` RFI contexts neutral when aggregating one source document. A document is now `MIXED` only when explicit Question and Response roles coexist (or an existing context is already `MIXED`), preventing `QUESTION + UNKNOWN` and `RESPONSE + UNKNOWN` from being falsely shown as a complete linked pair. The existing role sets and state machine are reused; no dependency or model call is introduced.
- Mark a Submittal group `AMBIGUOUS` when one source document contributes multiple distinct explicit statuses for the same normalized identifier. Previously the statuses were displayed together but collapsed into one set member before state evaluation, incorrectly producing `SINGLE`. The existing per-document status set and reviewer warning are reused; no winning status, contractual effect, dependency or model call is introduced.
- Preserve a complete spaced CSI-style Submittal identifier such as `23 05 00-01` when it exists only in the filename. The filename fallback now passes the full bounded candidate through the existing conservative normalizer instead of truncating it to `23`, so exact document/reference relationships form one group without fuzzy matching or inferred approval. The parser cache version advances; no dependency or model call is added.
- Preserve current HTML email text after a closed `blockquote` instead of permanently classifying everything after the first quote as historical. Paired quote markers also prevent an `On … wrote:` line inside the quote from leaking historical state into a later current reply. The existing Python `HTMLParser` path is reused; the parser cache version advances with no dependency, script execution, remote fetch or model call.
- Parse every exact Message-ID token in a multi-value `In-Reply-To` header instead of hashing the entire header as one nonexistent parent. The existing local hash and deterministic thread index now link all locally present parents into one review thread; raw identifiers remain absent, the parser cache version advances, and no dependency, model call or mailbox access is added.
- Filter ineligible RFI-question, rejected-Submittal, email-header and quoted-history citations individually when a material, QA item or property also has valid direct evidence. Preserve the valid candidate instead of confirming it with mixed evidence or deleting it wholesale, and derive conditional workflow status from every remaining direct source rather than only the first evidence ID. Reuse the existing deterministic quality and assembly paths with no dependency or provider call.
- Show persisted parent-email to selected-attachment provenance in the existing analysis-run workflow view. Repeated identical imports collapse to one visible relationship with an import count; attachment workflow role, approval and authority are never inherited. Add a SQLite upload-document index so the relationship lookup remains one bounded query. Replace raw workflow codes with readable labels in the page. Offline validation only; no model or external request.
- Reject keyword-only RFI/Submittal matches, normalize digit-bearing and spaced CSI-style identifiers plus common explicit Submittal review statuses, and preserve roleless RFI uncertainty. Add explicit selected EML attachment inspection/import through the existing upload pipeline with parent-email provenance. Nested attachments require another explicit selection; no model call, live provider request or automatic analysis is made. The implementation reuses Python stdlib email parsing, SQLite and the existing upload path with no new dependency.
- Add explicit email quoted-history separation, hash-only exact Message-ID relationships, and a bounded read-only RFI/Submittal/Email workflow view. Add customer-configured read-only Autodesk APS and Procore selected-file imports through existing project capacity, chunking, SHA-256 and deduplication. Reuse installed `httpx` and Windows DPAPI; do not add an SDK/runtime. Validation is synthetic MockTransport only and performs no live provider request, remote write or paid model call.
- Add local RFC-style EML parsing with separately locatable headers and safe visible body text, attachment inventory without implicit analysis, explicit RFI question/response and Submittal status evidence boundaries, and deterministic guards that prevent RFI questions or rejected Submittals from becoming current material/QA candidates. MSG, mailbox sync, attachment recursion and thread reconstruction remain unsupported; validation used synthetic offline files only and made no paid API call.
- Add deterministic PDF page routing, specification Section/Part/clause locators, bordered table-row evidence with coordinates, one bounded high-resolution schedule-table crop instead of an extra full-page call, stable crop recovery/cache identity, crop previews, and a document/locality check for property citations. Offline validation only; no paid API call or construction-accuracy claim.
- Add a measured offline PDF baseline, bounded 1/2/4-worker page processing for one large PDF, four-fragment/8.8 KB adjacent extraction batches with a durable recovery family, and deterministic material/QA quality checks. The 48-page generated fixture measured 10.213/8.929/7.496 seconds with identical outputs and estimated 136→34 extraction requests; no paid API was called and the absent customer PDFs are not represented as tested.
- Add a project budget editor: new projects default to CNY 300, users may select CNY 0.01–1,000,000, a limit cannot fall below settled plus outstanding cost, and every change is audit-recorded. A valid user increase can unlock a budget-frozen project without resetting its ledger.
- Add user-selectable 1/2/4 local document workers, selective whole-page vision routing, exact-evidence-scope verification batching, aggregate stage/provider/model timing, processed-page coverage, and a stage-weighted analysis percentage with elapsed-time estimated finish. Paid API requests remain serial; no live call was used to validate this change.
- 收紧材料与QA审核范围：材料/设备名称只保留实际实体，尺寸、厚度、gauge、材质、规格章节和明确数量进入独立字段；修复`equipment`、`one size`、`#5 bars @ 32 in O.C.`等属性或关键词误作名称。Tests & Inspections只保留可执行QA活动并分列Specification Section、Performer、Witness、Timing、Frequency和Acceptance Criteria；接线图、shop drawing、进度分析及普通submittal不再充当测试。当前UI及英文JSON/XLSX取消Conflict和Missing Information；历史存储不删除。对应证据优先截取实际支持句。保存运行离线重建为2,920条材料/设备和557条测试/检查，三张固定表通过英文、字段、公式、内部ID、格式噪声及渲染验收；未新增API调用。
- 按用户要求重构英文JSON/XLSX工程审核输出：材料或设备名称、数量和单位分列，名称中的英文数字/括号/最小数量前缀会与数量列分离；证据随项目显示，冲突双方值/来源/原文并排；移除内部ID、原始结构代码、独立证据/核验/引用表和PDF纸面几何审计。旧运行用本机Qwen离线兼容3,308个中文业务短语；缓存增加数字保真、异常通用说明及多源同译碰撞拒绝和缺译失败关闭。最终显示层清除非英文标点空壳、下划线字段与原始`null`。Excel每项显示最多3条对应证据短摘录，完整证据仍在JSON和应用中。当前10,926项重导出通过英文、行数、93条冲突、空白、公式、链接、ID、格式代码和实际渲染检查，未新增DeepSeek调用。
- 完成指定59页图纸与833页规格书的合并DeepSeek live终验：`RUN-7a2f9a8c090d40bdb802fdde17747d56`处理2/2文件、1,964条证据、222/222视觉页，发布10,926条待审核记录（3,224材料、1,785检查、93冲突、5,824缺失/非文档说明）并完成10,926份逐记录核验。8,501次调用全部结算，支出¥107.047278，预留/未知/未决均为0。完整JSON/XLSX已实际导出；引用关系、精确原文偏移、表行数和84,934个内部链接校验通过。221页PDF几何仅10页有比例标定且材料净量候选为0，终态`PARTIAL`继续要求人工审核。
- 按用户明确要求，本机DeepSeek/Gemini live启动器支持Windows DPAPI当前用户加密保存API密钥：后续自动加载，不写明文`.env`、网址、命令行、日志或Git；`--replace-key`强制更新，`--forget-key`删除。首次保存仍需本机密码表单与预算/数据发送确认。
- 修复大规格片段在请求前被旧6,000 UTF-8字节门槛拒绝，以及文本联合抽取先后在2,000和4,000生成token处截断的问题：抽取改为有界32,000字节工程包络和8,000生成token；视觉仍为2,000、字段核验仍为1,400。请求体、缓存参数和预算预留使用同一限额；人工重排后缀只作内部记账，模型与契约校验始终使用不可变原始Evidence ID。除BOM、完整JSON围栏或单对象前后短白名单化展示语外，新增保守契约修复：根级多余字段可移除，数字/布尔属性值保持为等值字符串，重复局部键和悬空父引用可降级修复，无法完整通过契约的原子项可舍弃；所有修复结果强制标记`TRUNCATED`并进入人工审核。多对象、未知前后文字、越界或缺失证据、审核结论字段及未知嵌套字段仍记账后失败关闭且不自动重试。
- 修复大规格书合并运行的本地发布与逐字段核验收尾：审核摘要限制为Schema允许的400字符，完整抽取属性仍保存在证据中；已完成模型调用而仅本地发布失败的运行可原地恢复，不重新发送已完成的付费任务。核验只加载记录实际引用的证据，分析发布不再重复预生成核验；模型回显的已知输入字段或过长理由可安全规范化，但一律降为`NEEDS_CONTEXT`，未知字段、错误路径和不精确原句继续失败关闭。
- DeepSeek V4 Flash Vision真实绘图运行`RUN-8091a26c122b40eeab3a5b4a87a6a5c7`完成59/59页：共220条证据片段，其中59条来自视觉（整体50个`EXTRACTED`、170个`NEEDS_REVIEW`）；599次调用、账本结算¥8.047433，预留/未知/未决均为0；生成379条待人工审核记录（79材料、26检查、274缺失）。逐字段核验为31条`SUPPORTED`、9条`PARTIAL`、13条`UNSUPPORTED`、52条`PENDING`和274条`NON_DOCUMENT`。该`PARTIAL`结果不代表施工准确率或自动材料净量；10页原始几何标定的`material_quantity`仍为空。
- 接入RapidOCR/ONNX本机OCR、DeepSeek V4 Flash Vision页面证据、PDF矢量审计、ezdxf对象解析及GNU LibreDWG本机DWG转换；指定59页PDF离线重解析新增17个OCR片段并补出53至58页，59页均进入视觉队列。CAD对象数量/已知单位几何只作为待人工审核候选，PDF整页矢量不映射材料时净量保持空；JSON/XLSX新增CAD量算和PDF几何审计。
- 按用户要求把下一次live复测切换为DeepSeek V4 Flash：新增不落盘Key的PowerShell与一次性本机网页入口，固定官方基址、`deepseek-v4-flash`和非思考JSON请求；用2026-09-11官方峰值美元价及10 CNY/USD安全倍数预留输入¥4.40、输出¥13.20，每次请求至少间隔1秒。配置与离线测试不调用API。
- DeepSeek独立项目已完整处理指定59页PDF的144/144文字片段并完成字段核验：243次调用全部结算¥3.109899、未决0，生成60条材料、16条检查、1条冲突和213条缺失/未处理记录，全部待人工审核。修复运行中网页每2.5秒重叠加载完整记录导致的CPU/内存放大：刷新改为串行，活跃运行不加载完整记录，终态只加载一次，人工操作后仍可强制刷新。
- 新增Google Gemini 3.6 Flash本机live适配：官方基址锁定、minimal推理、隐藏输出token保守记账及不落盘Key启动器；2026-09-11已完成合成连通测试（¥0.007605），并对59页真实PDF进行了首次运行和1次用户授权重试。两次PDF运行均因供应商HTTP错误以`PAUSED_PROVIDER`安全停止，重试最多到7/144个片段；PDF项目累计结算¥0.526795并保留¥0.237238未决预留，施工准确率仍未验证。
- 修复供应商HTTP错误只记录`HTTPStatusError`的问题：安全分类400/401/403/404/408/429/5xx，保留合规的`Retry-After`和供应商请求ID，不记录错误正文、文档内容或密钥；仍不自动重试或释放未决预留。
- 新增未决调用人工对账：用户必须先确认查看供应商账单，逐条记录未收费或实际人民币费用；原调用和不可变审计事件均保留。同项目还有任一未决调用时，网页开始、创建/处理/恢复分析及实际请求出口全部禁止新增付费工作；断点恢复只处理`PENDING`片段，不重做已完成解析或抽取。
- Gemini本机启动默认采用15秒最小请求开始间隔，以减少突发速率；不自动重试，不改变¥300项目预算，也不承诺绕过供应商分钟/日配额。
- 服务关停会把排队或运行中的字段核验标为`INTERRUPTED`；等待节流期间关停后不会再预留预算或发送请求。恢复聚合同一Tag材料时使用稳定记录身份；旧版记录就地迁移且保留人工编辑、主键、审核状态和版本。候选确因新证据变化时才递增审核版本、保留原审核事件并重置为待审核，避免旧结论或重复记录残留。
- TXT、DOCX和PDF文字片段同时受1,600字符及2,200 UTF-8字节边界约束，长中文行按Unicode字符安全拆分且原文不丢失，避免整段在API请求前因保守输入预算被拒绝。
- 无输出热修复：PDF解析周期保存页面进度，300秒有界超时后保留已完成片段；只有未处理项时网页自动显示该类别。
- 新增逐字段原句引用、源文件指纹和独立核验报告。
- 新增低价非思考字段复核、预算保护和持久化手动重验任务。
- 修改后的字段核验失效；不变字段按上下文/证据哈希复用。
- JSON/XLSX输出保留原句、字段核验及内部导航；中英文界面均支持核验面板。
- 新增幂等数据库表；保留旧项目/运行/审核/预算。除上述合成测试、真实PDF首次运行及用户授权重试外未调用真实模型；未部署远端，也未调用OCR/CAD/视觉API。

# v0.2.5 · 中英文显示切换
- 网页增加中文/English语言入口，动态提示和状态标签同时切换，偏好保存在浏览器。
- 原始项目资料、模型候选、证据、审核内容、数量、API与导出保持不变；语言切换不调用模型。
- 保留未保存编辑、筛选、上传和运行状态；补充手机英文布局及Cloudflare资源清单。
- 增加显示契约、语言组件和离线DOM回归；没有真实API调用或远端部署。

# v0.2.4 本机部署
- 不依赖GitHub/Render的隔离启动器，默认mock，显式live，端口冲突提示，后台线程/SQLite随应用运行。
- 已有远程模拟入口增加CMD包装及独立端口；不启用付费、无公网发布。
- 新增本机配置/启动器测试及真实HTTP、重启持久化验收记录；未验证事项独立说明。

# 0.2.3 · Render免费测试适配
新增独立mock云入口、显式free Blueprint、密码生成、精确Host、最小公开健康检查、易失性警示、合成示例下载、测试限额及回归。原本机业务与数据不迁移。平台发布未执行。

## 0.2.2 · Cloudflare受保护预览配置
新增鉴权、Pages Worker、白名单构建、远程启动脚本及离线测试。实际Cloudflare资源创建和公网验证未完成；默认mock，不调用付费模型。没有数据库迁移，不覆盖现有用户数据。

# v0.2.1：本地原型第一切片
新增运行应用、续传/持久化、基础解析、mock/文本API、预算、审核导出、Codex小任务工具和真实离线测试。未验证真实API或全项目施工能力；保持原规格目标与费用边界。没有修改用户远端。

# 变更记录
## 0.2.0 — 2026-09-08
按已澄清决定更新网页原型、全项目范围、互斥产品、设计净量、内部日期最新采用、仅阻断缺失、无供货责任判断、300CNY/24h/单项目、DeepSeek-first和低成本路由。
修复候选/审核/元数据契约；增加真实Prompt、Schema正负例、预算/日期/路由参考和更严格治理检查。正式Gold评测及跨Run人工修改继承暂缓。应用未实现，真实API未调用。

## 0.1.0
原开发规格草案。源归档哈希保存于baseline/source_manifest.json。原归档未修改。
