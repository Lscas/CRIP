{
  "status": "COMPLETE",
  "max_input_tokens": 8192,
  "max_output_tokens": 1400,
  "max_context_chars": 19000,
  "paid_calls_allowed": false,
  "approval_ref": "The active user goal requires continued efficiency, accuracy and adaptability improvements for RFI, Submittal and email content.",
  "notes": [
    "Primary routing accepts Submission as a Submittal name, but exact workflow-reference extraction currently recognizes only Submittal.",
    "An RFI or Email that says Submission No. 23-01 therefore loses an otherwise exact Submittal relationship.",
    "Extend the existing deterministic reference pattern and reuse the current identifier normalizer; add no dependency, semantic inference, mailbox access or model call.",
    "Offline parser and API regressions, specification sync and every bounded task context pack passed before completion."
  ],
  "task_id": "DEV-030",
  "title": "Submission alias workflow references",
  "requirement_ids": [
    "FR-PARSE-007",
    "FR-WORKFLOW-001"
  ],
  "allowed_paths": [
    "app/parsers.py",
    "tests/app/test_parsers.py",
    "tests/app/test_api.py",
    "spec/**",
    "docs/**",
    "tasks/DEV-030.json",
    "changes/CR-0045.json",
    "README.md",
    "CHANGELOG.md",
    "BUNDLE_MANIFEST.json"
  ],
  "context_files": [],
  "acceptance_tests": [
    "python -m pytest tests/app/test_parsers.py tests/app/test_api.py -q",
    "python scripts/run_checks.py --area parsers",
    "python scripts/run_checks.py --area api",
    "python scripts/run_checks.py --area all",
    "python scripts/check_spec_sync.py"
  ],
  "model_level": "review",
  "objective": "Preserve an exact digit-bearing Submittal relationship when a supported document uses the Submission alias."
}
