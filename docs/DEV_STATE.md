# 当前开发状态：v0.2.6 / evidence-verification

2026-09-15 email attachment workflow update: analysis-run workflow review now includes each persisted parent-email to selected-attachment relationship. Repeated imports of the same parent, content and attachment index collapse to one visible relationship with an import count, while the upload ledger retains every event. The relationship never inherits RFI/Submittal role, approval or authority. The endpoint uses one indexed SQLite query and no model call; the English page renders readable workflow labels instead of raw underscore codes. Synthetic offline API/UI/browser validation made zero external requests.

2026-09-16 workflow accuracy update: each Email MIME alternative group now selects one non-empty body, plain before safe HTML; conflicting same-type bodies warn instead of merging or manufacturing a MIXED role. Official Response maps to an RFI response; exact known RFI Status remains reviewer metadata across TXT/PDF/Email, and known Submittal responses reuse existing groups. Similar prose stays neutral, conflicts stay ambiguous, and no role, authority or precedence is inferred. Selected EML attachments reuse capacity/hash controls and remain inert until import. Existing Python/SQLite paths add no dependency, automatic analysis or external call. Checks use synthetic data.

2026-09-15 workflow/connectors update: EML current text and explicit quoted history are now separate evidence scopes; quoted history alone cannot create a current requirement or property. Exact RFI, Submittal and hashed Message-ID relationships are shown in a bounded read-only reviewer index, with duplicate/conflicting sources marked ambiguous and no model call. A customer can configure a read-only Autodesk APS or Procore bearer connection locally, browse project folders, and import selected files through the existing project capacity, 4 MiB chunks, SHA-256 and deduplication path. Optional persistence reuses Windows current-user DPAPI; status and API responses never return tokens, and cross-host signed downloads do not receive bearer credentials. Validation used synthetic `httpx.MockTransport` only; no Autodesk/Procore tenant, customer file, paid model or remote write was used. Interactive OAuth, token refresh, provider-side revocation, live tenant acceptance, recursive bulk import, remote synchronization, MSG and semantic email-thread inference remain planned.
2026-09-15 RFI/Submittal/Email update: RFC-style `.eml` files now parse locally into separately locatable headers and preferred plain-text or safe visible HTML body evidence. The parser does not execute HTML or fetch remote resources; attachments are inventoried as `NOT_PROCESSED` and require separate upload. Explicit RFI question/response and Submittal status labels are preserved in evidence locators. Deterministic checks prevent RFI questions and rejected/revise-and-resubmit Submittals from becoming current material or QA candidates; response, Submittal and Email-source candidates remain conditional and require engineer review. Synthetic offline parser/quality tests passed. MSG, mailbox connections, attachment recursion, quoted-message/thread reconstruction and live construction-accuracy evaluation remain planned; no paid API or customer document was used.
2026-09-15 PDF structure/crop update: native PDF pages now record a deterministic page type and retain detected specification Section, Part, clause, and bordered table-row context in evidence locators. Eligible schedule/spec table pages use one bounded 2,560-pixel local crop with full-page coordinates, stable recovery/cache identity and a matching preview; other visual pages retain one overview request, so this change does not multiply calls per page. Property citations from another document or a non-local evidence range are removed by deterministic quality checks. The full offline Python/JavaScript suite, spec sync and source manifest pass. A repeated 48-page fixture retained 136 fragments, 34 estimated extraction batches and identical 1/2/4-worker hashes at 10.351/9.033/8.499 seconds. No paid API or customer document was used. Borderless/cross-page tables, multi-column reading order, general drawing-region detection and construction-accuracy evaluation remain planned.
2026-09-15 Performance/quality controls: runs expose 1/2/4 local workers, stage timing, processed pages, percentage and ETA; one PDF now uses bounded four-page process chunks. A generated 48-page fixture measured 10.213/8.929/7.496 seconds with identical output hashes; adjacent extraction groups reduced the estimated request count from 136 to 34. Deterministic checks remove clear material/QA false positives and evidence-scope errors. Vision remains selective, verification batches exact evidence scope, and paid calls remain serial. Project limits are user-selected CNY 0.01–1,000,000 (CNY 300 default) without ledger reset. Missing customer PDFs make this a fixture timing—not a customer-file or accuracy benchmark. No paid API, credential, `.local` data, or saved project was used.
2026-09-14 Tooling/model entry: `run_checks.py --area all` is the single local/CI check and fails if Node.js is missing. `start-custom-model.ps1` accepts an OpenAI-compatible remote text API (HTTPS/key/positive rates) or loopback local model (HTTP/no key/zero rates); endpoint/model fingerprinting blocks cross-target resume. Generic vision and vendor-specific parameters remain disabled. Offline tests made no model call.
2026-09-13 Focused material and QA reviewer fix: material/equipment names now identify tangible entities instead of dimensions, gauges, headings, generic keywords, or descriptive clauses; size, thickness, material, rating, specification section, and explicit quantity remain separate fields. Historical examples such as `equipment`, `one size`, `#5 bars @ 32 in O.C.`, `18 gauge`, insulation-thickness headings, and water-service material/size headings are repaired or rejected. Tests & Inspections now retain only executable QA activities, with Specification Section, Performer, Witness, Timing, Frequency, Acceptance Criteria and focused supporting evidence on the same row; wiring diagrams, shop drawings, schedules, time-impact analysis and ordinary submittals are excluded. Conflict and Missing Information are removed from current assembly, reviewer UI and readable exports without deleting legacy stored data. The saved run was rebuilt offline as a 3,713,667-byte JSON file and a 568,206-byte XLSX file with Summary, Materials & Equipment, and Inspections & Tests only: 2,920 material/equipment items and 557 test/inspection items. Validation reports zero invalid example names, unrelated QA documents, Chinese output text, internal IDs, formatting artifacts, raw nulls, formulas, hyperlinks, or whitespace-only cells. All sheets were inspected with the spreadsheet artifact runtime and rendered. The full offline suite and 23 UI localization tests pass. The live loopback app was restarted on port 8010 with the saved encrypted DeepSeek credential, without starting analysis. The ledger remains 8,501 calls, ¥107.047278 spent, and zero reserved, unknown, or unresolved cost.
2026-09-13视觉/CAD与大规格书合并终验：接入RapidOCR 3/ONNX本机OCR、DeepSeek `deepseek-v4-flash-vision-exp`整页视觉证据、PDF矢量审计、ezdxf对象量算与GNU LibreDWG 0.14本机DWG转换。运行`RUN-7a2f9a8c090d40bdb802fdde17747d56`完整处理指定59页图纸和833页规格书：2/2文件、1,964条证据（1,724文字层、18 OCR、222视觉）、222/222视觉页和10,926/10,926逐记录核验全部收口。四类待审核记录为3,224材料、1,785检查、93冲突和5,824缺失/非文档说明，人工状态全部`PENDING`。8,501次DeepSeek调用全部结算，账本支出¥107.047278，预留/未知/未决均为0。完整JSON/XLSX已实际导出并通过引用、原文偏移、行数、84,934个内部链接和公式注入安全检查。221页PDF几何仅10页有比例标定，材料净量候选为0；`PARTIAL`结果不等于施工准确率或可采购数量。
新增字段引用与独立核验，不改变原文、业务批准状态或预算。见docs/EVIDENCE_VERIFICATION.md与docs/EVIDENCE_VALIDATION.md。
定向：python scripts/run_checks.py --area evidence；全套：--area all。新表自动幂等创建，不删除.local/.env。GET不发送模型请求。
2026-09-12 The user explicitly requested saved credentials to avoid repeated entry. Windows DPAPI current-user encrypted storage and provider-separated automatic loading are implemented; DeepSeek/Gemini launchers do not write the key to a plaintext `.env`, URL, command line, log, or Git. `--replace-key` and `--forget-key` are supported; a different Windows user or computer still requires entry again. The current DeepSeek key is authorized for encrypted local loading without reading or echoing its value. The current baseline has 485 offline tests, 100 requirements, and 15 schemas.
2026-09-11用户要求把修复后的下一次live复测切换为DeepSeek V4 Flash。已新增`start-deepseek-live.ps1`和`scripts/deepseek_local_setup.py`，固定官方`https://api.deepseek.com`、模型`deepseek-v4-flash`、`thinking=disabled`，并按当前官方峰值美元价及10 CNY/USD安全倍数预留输入¥4.40/百万、输出¥13.20/百万。独立DeepSeek项目已完整处理指定59页PDF的144/144文字片段，生成60条材料、16条检查、1条冲突和213条缺失/未处理记录；243次调用全部结算¥3.109899，未决0。原Gemini历史调用、预算与两条未决费用保持原样，不因切换供应商自动结算或删除。该结果不包含OCR、视觉、CAD、几何数量或施工准确率验证。
2026-09-11已在本机用Gemini 3.6 Flash完成三次有界验证。合成连通项目：1次调用、839输入token、35输出token、账本支出¥0.007605、未知调用0。真实59页PDF项目：上传文件与原件SHA-256一致，解析为144个片段；首次运行在6/144后暂停，用户明确授权的第二次运行在7/144后再次因`HTTPStatusError`进入`PAUSED_PROVIDER`。PDF项目累计12条模型调用记录、2条未决，已结算¥0.526795、保留预留¥0.237238，可用¥299.235967；第二次运行仍有137个片段未处理。该结果验证了真实PDF上传、文本解析、局部真实调用和失败关闭记账，也排除了本地6,000输入预算是这两次暂停的直接原因；不验证完整分析或施工准确率。mock仍只展示原文定位且保留待语义核验。全项目无漏项、原生CAD、OCR、准确率未验证。原DEV-002产品选项映射仍待开发，不以本轮字段核验冒充其实现。

重复HTTP故障后已修复诊断丢失：`Gateway`对400、401、403、404、408、429及5xx持久化短结构化分类，并只接受格式受限的`Retry-After`和`x-request-id`/`x-goog-request-id`；响应正文、请求正文与密钥不写入诊断。提取和核验共用相同路径，费用继续保持未决预留且不自动重试。

恢复链路现已补全：网页列出项目未决调用，只有用户明确确认已查看供应商账单，才能将单条调用登记为未收费或输入实际人民币费用；原调用不删除，并新增`call_reconciliation_events`审计事件。项目任一运行仍有未决调用时，网页开始、创建、队列处理、恢复和Gateway/预算预留均拒绝新增付费工作。恢复后跳过已有`document_results`和非`PENDING`证据；同一Tag材料用稳定组键保持记录身份。旧版记录按审核历史中的系统生成基线就地迁移，人工编辑内容、记录ID、审核状态和版本均保留；只有新增证据改变系统候选时才递增审核版本、保留旧审核事件并重置为待审核。进程中断遗留的`RESERVED`调用在下一次启动变为可核对的`UNKNOWN`，不会永久卡死。Gemini从环境启动时默认两次请求开始至少间隔15秒；服务关停会中断排队/运行中的核验，节流等待结束后也不会再预留或发送。没有自动重试或配额绕过。长中文行按2,200 UTF-8字节的完整字符边界拆分，保留原文且不提高6,000单片段输入门槛。

DeepSeek全量复测暴露网页运行轮询重叠加载全部记录的问题：运行中每2.5秒的完整记录请求会并发堆积，放大CPU与内存并拖慢本地收尾。现已串行化刷新，活跃运行只读轻量状态/费用/未决调用，终态只加载一次完整记录；人工审核和手动核验后仍强制刷新。旧进程在运行已结束、243次调用全部结算且未决0后停止，积压读取被取消，没有删除或重跑结果。

合并运行的本地发布阶段又暴露了超长审核摘要会超过候选Schema上限。现只截短显示用摘要至400字符，完整属性仍保留在证据抽取中；本地发布失败可在无待抽取证据时恢复，不重发已完成模型调用。字段核验改为按实际引用ID取证据，并移除发布阶段的重复预核验。DeepSeek偶尔回显输入字段或给出过长理由时，只做无事实新增的规范化并强制降为`NEEDS_CONTEXT`；未知字段、错误路径或不精确原句仍记为`MODEL_OUTPUT_REJECTED`。
