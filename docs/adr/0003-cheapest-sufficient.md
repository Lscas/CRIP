# ADR-0003 最低足够成本执行
决定依据：用户要求低级模型处理简单任务，避免过多token。

先用代码完成确定性任务，L1为Flash非思考，L2为同Flash低effort；视觉独立，L3关闭。按事实联合抽取减少多Agent重复全文，不以少读文件省钱。预算为用户选择的项目累计上限（新项目默认300CNY）、24小时目标、单项目；超过约束明确暂停。

低价模型不天然少tokens，因此同时控制输入范围、结果缓存、思考、输出长度和重试。实际费用需用户API验证。本轮仅离线规则，不付费运行。
