# ADR-0001 API-first（修订版）
状态：按本轮用户决定更新；规格0.2.0。

初期使用用户提供的DeepSeek兼容API，不从零训练。统一ModelGateway；原文件和索引由产品持有；默认cheap非思考，图像经专用能力探针。真正高级模型需批准启用。每个运行保留配置、Prompt、价格和来源快照。

与旧ADR区别：不再强制历史Gold数据作为原型门槛；不假定供应商Batch或文件检索接口存在；模型/视觉/预算能力先小探针。正式商业质量验证另行批准。
